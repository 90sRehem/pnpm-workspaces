describe("Contract tests", () => {
  beforeAll(() => {

  });

  it("should return error if value is null", () => {
    expect(false).toBe(true);
  });

  it("should return success if value is not null", () => {
    expect(false).toBe(true);
  });

  it("should return error if string is empty", () => {
    expect(false).toBe(true);
  });

  it("should return success if string is not null", () => {
    expect(false).toBe(true);
  });

  it("should return error if string is empty", () => {
    expect(false).toBe(true);
  });

  it("should return success if string is not null", () => {
    expect(false).toBe(true);
  });

  it("should return error if string is null or empty", () => {
    expect(false).toBe(true);
  });

  it("should return success if string is not null or empty", () => {
    expect(false).toBe(true);
  });

  it("should return error if values are not equal", () => {
    expect(false).toBe(true);
  });

  it("should return success if values are equal", () => {
    expect(false).toBe(true);
  });

  it("should return error if number is not between max and min", () => {
    expect(false).toBe(true);
  });

  it("should return success if number is between max and min", () => {
    expect(false).toBe(true);
  });

  it("should return error if length is not between max and min", () => {
    expect(false).toBe(true);
  });

  it("should return success if length is between max and min", () => {
    expect(false).toBe(true);
  });

  it("should return success if given value is less then max length", () => {
    expect(false).toBe(true);
  });

  it("should return error if given value is bigger then max length", () => {
    expect(false).toBe(true);
  });

  it("should return error if given value is less then min length", () => {
    expect(false).toBe(true);
  });

  it("should return success if given value is bigger or equal then min length", () => {
    expect(false).toBe(true);
  });

  it("should return error if a number is less then other number", () => {
    expect(false).toBe(true);
  });

  it("should return success if a number is bigger then other number", () => {
    expect(false).toBe(true);
  });

  it("should return success if a number is less then other number", () => {
    expect(false).toBe(true);
  });

  it("should return error if a number is bigger then other number", () => {
    expect(false).toBe(true);
  });

  it("should return success if a value have exactly length then given size", () => {
    expect(false).toBe(true);
  });

  it("should return error if a value dont have exactly length then given size", () => {
    expect(false).toBe(true);
  });

  it("should return success if an email is valid", () => {
    expect(false).toBe(true);
  });

  it("should return error if an email is invalid", () => {
    expect(false).toBe(true);
  });

  it("should return success if an url is valid", () => {
    expect(false).toBe(true);
  });

  it("should return error if an url is invalid", () => {
    expect(false).toBe(true);
  });

  it("should return error if an url is valid", () => {
    expect(false).toBe(true);
  });

  it("should return success if an url is invalid", () => {
    expect(false).toBe(true);
  });

  it("should return error if a value is not alphanumeric", () => {
    expect(false).toBe(true);
  });

  it("should return success if a value is alphanumeric", () => {
    expect(false).toBe(true);
  });

  it("should return error if a value is not a number", () => {
    expect(false).toBe(true);
  });

  it("should return success if a value is a number", () => {
    expect(false).toBe(true);
  });
});
